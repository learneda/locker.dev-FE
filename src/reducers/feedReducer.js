import {} from '../actions/types'

const initialState = {
  courses: [],
  articles: [],
  error: null,
}
export const feedReducer = (state = initialState, action) => {
  switch (action.type) {
  }
}
